{
  "code": 200,
  "data": {
    "description": "",
    "html": "<html><head><meta name=\"color-scheme\" content=\"light dark\"></head><body><pre style=\"word-wrap: break-word; white-space: pre-wrap;\">'use client';\n\nimport { useState, useEffect } from 'react';\nimport { motion, AnimatePresence } from 'framer-motion';\nimport { LayoutDashboard, Pill, Shield, Heart, MapPin } from 'lucide-react';\nimport { useRecoveryStore } from '@/lib/store';\n\nimport Onboarding from '@/components/recovery/Onboarding';\nimport CrisisCamouflage from '@/components/recovery/CrisisCamouflage';\nimport CrisisButton from '@/components/recovery/CrisisButton';\nimport Dashboard from '@/components/recovery/Dashboard';\nimport SubstanceModules from '@/components/recovery/SubstanceModules';\nimport SSSEngine from '@/components/recovery/SSSEngine';\nimport StackVisualizer from '@/components/recovery/StackVisualizer';\nimport NeuroTimeline from '@/components/recovery/NeuroTimeline';\nimport RecoveryFoundations from '@/components/recovery/RecoveryFoundations';\nimport MindPsychology from '@/components/recovery/MindPsychology';\nimport PHContext from '@/components/recovery/PHContext';\n\ntype MainTab = 'dashboard' | 'substances' | 'bio_tools' | 'recovery' | 'ph_guide';\ntype BioSubTab = 'sss' | 'stack' | 'timeline';\ntype RecoverySubTab = 'foundations' | 'mind';\n\nconst NAV_ITEMS: { id: MainTab; label: string; icon: typeof LayoutDashboard }[] = [\n  { id: 'dashboard', label: 'Home', icon: LayoutDashboard },\n  { id: 'substances', label: 'Substances', icon: Pill },\n  { id: 'bio_tools', label: 'Bio Tools', icon: Shield },\n  { id: 'recovery', label: 'Recovery', icon: Heart },\n  { id: 'ph_guide', label: 'PH Guide', icon: MapPin },\n];\n\nfunction BioToolsView() {\n  const [sub, setSub] = useState<BioSubTab>('sss');\n  const tabs: { id: BioSubTab; label: string }[] = [\n    { id: 'sss', label: 'Safety Check' },\n    { id: 'stack', label: 'Stack Builder' },\n    { id: 'timeline', label: 'Timeline' },\n  ];\n\n  return (\n    <div>\n      <div className=\"flex gap-2 px-4 pt-3\">\n        {tabs.map(t => (\n          <button\n            key={t.id}\n            onClick={() => setSub(t.id)}\n            className={`px-3 py-2 rounded-xl text-xs font-medium transition-colors ${\n              sub === t.id ? 'bg-[#0066CC] text-white' : 'bg-[#1E293B] text-[#94A3B8] border border-[#334155]'\n            }`}\n          >\n            {t.label}\n          </button>\n        ))}\n      </div>\n      <AnimatePresence mode=\"wait\">\n        {sub === 'sss' && <motion.div key=\"sss\" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><SSSEngine /></motion.div>}\n        {sub === 'stack' && <motion.div key=\"stack\" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><StackVisualizer /></motion.div>}\n        {sub === 'timeline' && <motion.div key=\"timeline\" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><NeuroTimeline /></motion.div>}\n      </AnimatePresence>\n    </div>\n  );\n}\n\nfunction RecoveryView() {\n  const [sub, setSub] = useState<RecoverySubTab>('foundations');\n  const tabs: { id: RecoverySubTab; label: string }[] = [\n    { id: 'foundations', label: 'Foundations' },\n    { id: 'mind', label: 'Mind & Psychology' },\n  ];\n\n  return (\n    <div>\n      <div className=\"flex gap-2 px-4 pt-3\">\n        {tabs.map(t => (\n          <button\n            key={t.id}\n            onClick={() => setSub(t.id)}\n            className={`px-3 py-2 rounded-xl text-xs font-medium transition-colors ${\n              sub === t.id ? 'bg-[#0066CC] text-white' : 'bg-[#1E293B] text-[#94A3B8] border border-[#334155]'\n            }`}\n          >\n            {t.label}\n          </button>\n        ))}\n      </div>\n      <AnimatePresence mode=\"wait\">\n        {sub === 'foundations' && <motion.div key=\"foundations\" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><RecoveryFoundations /></motion.div>}\n        {sub === 'mind' && <motion.div key=\"mind\" initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }}><MindPsychology /></motion.div>}\n      </AnimatePresence>\n    </div>\n  );\n}\n\nexport default function Home() {\n  const { isOnboarded, isCrisisMode } = useRecoveryStore();\n  const [activeTab, setActiveTab] = useState<MainTab>('dashboard');\n\n  // Register service worker\n  useEffect(() => {\n    if ('serviceWorker' in navigator) {\n      navigator.serviceWorker.register('/sw.js').catch(() => {\n        // Service worker registration failed - app still works\n      });\n    }\n  }, []);\n\n  // Show onboarding if not completed\n  if (!isOnboarded) {\n    return <Onboarding />;\n  }\n\n  // Show crisis camouflage if activated\n  if (isCrisisMode) {\n    return <CrisisCamouflage />;\n  }\n\n  return (\n    <div className=\"min-h-screen bg-[#0F172A] flex flex-col\">\n      {/* Main Content */}\n      <main className=\"flex-1 overflow-y-auto pb-24\" role=\"main\">\n        <AnimatePresence mode=\"wait\">\n          {activeTab === 'dashboard' && (\n            <motion.div key=\"dashboard\" initial={{ opacity: 0, y: 5 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -5 }} transition={{ duration: 0.15 }}>\n              <Dashboard />\n            </motion.div>\n          )}\n          {activeTab === 'substances' && (\n            <motion.div key=\"substances\" initial={{ opacity: 0, y: 5 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -5 }} transition={{ duration: 0.15 }}>\n              <SubstanceModules />\n            </motion.div>\n          )}\n          {activeTab === 'bio_tools' && (\n            <motion.div key=\"bio_tools\" initial={{ opacity: 0, y: 5 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -5 }} transition={{ duration: 0.15 }}>\n              <BioToolsView />\n            </motion.div>\n          )}\n          {activeTab === 'recovery' && (\n            <motion.div key=\"recovery\" initial={{ opacity: 0, y: 5 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -5 }} transition={{ duration: 0.15 }}>\n              <RecoveryView />\n            </motion.div>\n          )}\n          {activeTab === 'ph_guide' && (\n            <motion.div key=\"ph_guide\" initial={{ opacity: 0, y: 5 }} animate={{ opacity: 1, y: 0 }} exit={{ opacity: 0, y: -5 }} transition={{ duration: 0.15 }}>\n              <PHContext />\n            </motion.div>\n          )}\n        </AnimatePresence>\n      </main>\n\n      {/* Crisis Button */}\n      <CrisisButton />\n\n      {/* Bottom Navigation */}\n      <nav\n        className=\"fixed bottom-0 left-0 right-0 z-40 bg-[#1E293B] border-t border-[#334155]\"\n        role=\"navigation\"\n        aria-label=\"Main navigation\"\n        style={{ paddingBottom: 'env(safe-area-inset-bottom, 0px)' }}\n      >\n        <div className=\"flex items-center justify-around h-16 max-w-md mx-auto\">\n          {NAV_ITEMS.map(item => {\n            const isActive = activeTab === item.id;\n            return (\n              <button\n                key={item.id}\n                onClick={() => setActiveTab(item.id)}\n                className=\"flex flex-col items-center justify-center w-full h-full gap-0.5 touch-target transition-colors\"\n                role=\"tab\"\n                aria-selected={isActive}\n                aria-label={item.label}\n              >\n                <item.icon\n                  className={`w-5 h-5 transition-colors ${isActive ? 'text-[#0066CC]' : 'text-[#94A3B8]'}`}\n                  strokeWidth={isActive ? 2.5 : 2}\n                />\n                <span className={`text-[10px] font-medium transition-colors ${isActive ? 'text-[#0066CC]' : 'text-[#94A3B8]'}`}>\n                  {item.label}\n                </span>\n              </button>\n            );\n          })}\n        </div>\n      </nav>\n    </div>\n  );\n}\n</pre></body></html>",
    "title": "",
    "url": "https://raw.githubusercontent.com/marktantongco/RecoveryLine/main/src/app/page.tsx",
    "usage": {
      "tokens": 2014
    }
  },
  "meta": {
    "usage": {
      "tokens": 2014
    }
  },
  "status": 20000
}